{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch App",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/app.js",  // Verifique se este caminho está correto
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}
